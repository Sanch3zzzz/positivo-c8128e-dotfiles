# systemd-oomd: mata o processo guloso no user.slice quando a memoria aperta,
# em vez de congelar o sistema (Floorp + thunar em 7,5GB).
[Slice]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=60