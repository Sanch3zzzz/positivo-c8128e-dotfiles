# Swap comprimido em RAM (zram): mais rapido que swap em eMMC e sem desgastar
# o flash. Prioridade maior (100) que o swap de disco (padrao) -> zram primeiro,
# a particao de 4G fica como reserva em pressao extrema.
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
swap-priority = 100